@page
@model Advertisement_LTd.Pages.Candidates.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Candidate[0].Name_of_candidate)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Candidate[0].DOB_of_candidate)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Candidate[0].Mobile_no_of_candidate)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Candidate[0].Email_address_of_candidate)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Candidate) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Name_of_candidate)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DOB_of_candidate)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Mobile_no_of_candidate)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Email_address_of_candidate)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.Id">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
